
<div>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="">
                ICARUS
              </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Admission
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
                            <a class="dropdown-item" id="admissionbrowse" >Admission Browse</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" id="residentbrowse">Resident Browse</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" id="inquirylog">Inquiry Log</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" id="payments">Payments</a>
</div>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Expenses
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
                            <a class="dropdown-item" id="expensebrowse">Expense Browse</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item"  id="vendors">Vendors</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" id="requests">Requests</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item"  id="assertions">Assertion</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" id="chartofaccounts"> Chart of Accounts</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown3" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Reports
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown3">
                            <a class="dropdown-item" id="reports">Reports</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item"  id="residentactivity">Resident Activity</a>
                        </div>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown3" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Accounts
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown4">
                            <a class="dropdown-item" href="#" id="logout">Logout</a>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</div>
<script>
    const $dropdown = $(".dropdown");
    const $dropdownToggle = $(".dropdown-toggle");
    const $dropdownMenu = $(".dropdown-menu");
    const showClass = "show";

    $(window).on("load resize", function () {
        if (this.matchMedia("(min-width: 768px)").matches) {
            $dropdown.hover(
                function () {
                    const $this = $(this);
                    $this.addClass(showClass);
                    $this.find($dropdownToggle).attr("aria-expanded", "true");
                    $this.find($dropdownMenu).addClass(showClass);
                },
                function () {
                    const $this = $(this);
                    $this.removeClass(showClass);
                    $this.find($dropdownToggle).attr("aria-expanded", "false");
                    $this.find($dropdownMenu).removeClass(showClass);
                }
            );
        } else {
            $dropdown.off("mouseenter mouseleave");
        }
    });
    //Admission
     $("#admissionbrowse").click(function () {
    window.location = '@Url.Action("Index", "Admissions")';
    });
     $("#residentbrowse").click(function () {
    window.location = '@Url.Action("Index", "Resident")';
    });
     $("#inquirylog").click(function () {
    window.location = '@Url.Action("Index", "Inquiries")';
    });
     $("#payments").click(function () {
    window.location = '@Url.Action("Index", "Payments")';
    });
    //Expenses
     $("#expensebrowse").click(function () {
    window.location = '@Url.Action("Index", "Expens")';
    });
     $("#vendors").click(function () {
    window.location = '@Url.Action("Index", "Vendors")';
    });
     $("#requests").click(function () {
    window.location = '@Url.Action("Index", "Requests")';
    });
     $("#assertions").click(function () {
    window.location = '@Url.Action("Index", "Assertions")';
    });
    $("#chartofaccounts").click(function () {
    window.location = '@Url.Action("Index", "ChartofAccounts")';
    });
    //Reports
      $("#reports").click(function () {
    window.location = '@Url.Action("Index", "Reports")';
    });
     $("#residentactivity").click(function () {
    window.location = '@Url.Action("Index", "ResidentActivities")';
    });
 
</script>


<style>
    .tableheadleft {
        border-top-left-radius: 15px;
    }
    .tableheadright {
        border-top-right-radius: 15px;
    }
    body {
        background: #f2f2f2;
    }

    .navbar .nav-item:not(:last-child) {
        margin-right: 35px;
    }

    .dropdown-toggle::after {
        transition: transform 0.15s linear;
    }

    .show.dropdown .dropdown-toggle::after {
        transform: translateY(3px);
    }

    .dropdown-menu {
        margin-top: 0;
    }
</style>